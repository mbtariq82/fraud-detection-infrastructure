[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fraud-detection-infrastructure"
version = "0.1.0"
description = "Training fraud detection API with feature scoring, rules, cases, and feedback."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1.0",
    "uvicorn[standard]>=0.30,<1.0",
]

[project.optional-dependencies]
dev = [
    "httpx2>=2.0,<3.0",
    "pytest>=8.0,<9.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/fraud_detection_infrastructure"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
